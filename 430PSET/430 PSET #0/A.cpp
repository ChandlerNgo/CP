#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n; int p;
    cin >> n >> p;
    for(int i = 0; i < n; i++){
        string s;
        cin >> s;
    }
    cout << p << endl;
}