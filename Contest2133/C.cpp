#include <bits/stdc++.h>
typedef long long ll;
typedef long double ld;
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // record path lengths with all starts with other nodes

    // for each path length, figure out which node is next
    // with path length P, does a and b(pathlength-1) form path length 2? if yes then it is next node
}